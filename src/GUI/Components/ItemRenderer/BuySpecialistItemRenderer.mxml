<?xml version="1.0" encoding="utf-8"?>
<mx:Canvas xmlns:mx="http://www.adobe.com/2006/mxml" xmlns:local="GUI.Components.ItemRenderer.*" xmlns:ui="GUI.Components.*" xmlns:core="mx.core.*" width="230">
    <mx:Script><![CDATA[
        import mx.containers.Canvas;
        import GUI.Components.Frame;
        import GUI.Components.CustomLabel;
        import __AS3__.vec.Vector;
        import ServerState.dResource;
        import GUI.Components.StandardButton;
        import mx.core.mx_internal;
        import ServerState.cResources;
        import Communication.VO.dRequirementsVO;
        import ServerState.cPlayerData;
        import Specialists.cSpecialistDescription;
        import Specialists.cSpecialist;
        import Enums.SPECIALIST_TYPE;
        import Interface.cGameInterface;
        import GUI.Components.ToolTips.cToolTipUtil;
        import mx.events.PropertyChangeEvent;
        import mx.events.FlexEvent;
        import GUI.Loca.cLocaManager;
        import Enums.LOCA_GROUP;
        import flash.events.Event;
        import GUI.Assets.gAssetManager;
        import mx.core.ClassFactory;
        import mx.events.ListEvent;
        import mx.events.ToolTipEvent;
        import flash.events.MouseEvent;
        import flash.events.*;
        import flash.display.*;
        import flash.geom.*;
        import mx.styles.*;
        import flash.text.*;
        import flash.media.*;
        import mx.binding.*;
        import flash.filters.*;
        import flash.utils.*;
        import flash.net.*;
        import flash.system.*;
        import flash.accessibility.*;
        import flash.xml.*;
        import flash.ui.*;
        import flash.external.*;
        import flash.desktop.*;
        import flash.data.*;
        import flash.debugger.*;
        import flash.errors.*;
        import flash.filesystem.*;
        import flash.html.*;
        import flash.html.script.*;
        import flash.printing.*;
        import flash.profiler.*;
        import GUI.Components.RendererPresentationUpdates;
        import flash.events.IEventDispatcher;

        private static var lastTimeUpdated:Number = 0;

        private var toolTipClass:String = "Simple";

        private var costsToBuy:Vector.<dResource>;

        private var toolTipData:Object;

        private function setCosts():void
        {
            var localValue4:cResources;
            var localValue5:dRequirementsVO;
            var localValue1:cPlayerData = global.ui.mCurrentPlayer;
            var localValue2:cSpecialistDescription = (data as cSpecialistDescription);
            cSpecialist.GetCostsToBuy_vector(localValue2.GetType(), localValue1.GetSpecialistAmount(localValue2.GetType()));
            var localValue3:Vector.<dResource> = cSpecialist.GetCostsToBuy_vector(localValue2.GetType(), localValue1.GetSpecialistAmount(localValue2.GetType()));
            if (localValue3)
            {
                this.costsToBuy = localValue3;
                this.costsList.dataProvider = this.costsToBuy;
                localValue4 = global.ui.mCurrentPlayerZone.GetResources(global.ui.mCurrentPlayer);
                localValue5 = (global.ui as cGameInterface).mRequirements.itemRequirements_vector[SPECIALIST_TYPE.toString(localValue2.GetType())];
                if (!localValue5.isFulfilled())
                {
                    this.btnBuy.enabled = false;
                    this.btnBuy.toolTip = localValue5.locaString;
                    this.toolTipClass = cToolTipUtil.SIMPLE_ERROR_string;
                    this.toolTipData = true;
                }
                else
                {
                    this.btnBuy.enabled = localValue4.HasPlayerResourcesInListOne(this.costsToBuy);
                    this.toolTipClass = cToolTipUtil.SIMPLE_string;
                    this.toolTipData = null;
                };
            }
            else
            {
                this.btnBuy.enabled = false;
                this.toolTipClass = cToolTipUtil.SIMPLE_string;
                this.toolTipData = null;
            };
        }

        private function displayData(argument1:Event=null):void
        {
            var localValue2:String;
            this.removeEventListener(FlexEvent.CREATION_COMPLETE, this.displayData);
            localValue2 = ((data) ? SPECIALIST_TYPE.toString((data as cSpecialistDescription).GetType()) : "");
            this.btnBuy.toolTip = cLocaManager.GetInstance().GetText(LOCA_GROUP.LABELS, "BuySpecialist", [localValue2]);
            this.specialistType.text = cLocaManager.GetInstance().GetText(LOCA_GROUP.SPECIALISTS, localValue2);
            this.specialistIcon.content = (("icon_" + localValue2.toLowerCase()) + ".png");
            this.setCosts();
        }

        public function refresh():void
        {
            this.setCosts();
        }

        override public function set data(argument1:Object):void
        {
            if (!rendererReady)
            {
                pendingRendererData = argument1;
                hasPendingRendererData = true;
                return;
            }
            trace("BuySpecialistItemRenderer::data");
            super.data = argument1;
            if ((((this.specialistIcon) && (this.btnBuy)) && (this.specialistType)))
            {
                this.displayData();
            }
            else
            {
                this.addEventListener(FlexEvent.CREATION_COMPLETE, this.displayData);
            };
            refreshRendererPresentation();
        }

        private function Buy():void
        {
            this.btnBuy.enabled = false;
            dispatchEvent(new ListEvent("BuySpecialist", true, false, -1, data.GetType()));
        }

        public function onBtnBuy_toolTipCreate(argument1:ToolTipEvent):void
        {
            cToolTipUtil.createToolTip(this.toolTipClass, argument1, this.toolTipData);
        }

        public function onBtnBuy_click(argument1:MouseEvent):void
        {
            this.Buy();
        }

        private var rendererReady:Boolean = false;
        private var pendingRendererData:Object;
        private var hasPendingRendererData:Boolean = false;
        private var rendererPresentationUpdating:Boolean = false;
        private var rendererUpdates:RendererPresentationUpdates;

        override protected function childrenCreated():void
        {
            super.childrenCreated();
            btnBuy.addEventListener("click", onBtnBuy_click);
            btnBuy.addEventListener("toolTipCreate", onBtnBuy_toolTipCreate);
            viewClassFactory1.generator = ResourceItemRenderer;
            costsList.itemRenderer = viewClassFactory1;
            this.width = 230;
            rendererReady = true;
            rendererUpdates = new RendererPresentationUpdates(this, refreshRendererPresentation);
            if (hasPendingRendererData)
            {
                var initialData:Object = pendingRendererData;
                hasPendingRendererData = false;
                pendingRendererData = null;
                data = initialData;
            }
            refreshRendererPresentation();
        }

        private function refreshRendererPresentation():void
        {
            if (!rendererReady || rendererPresentationUpdating) return;
            rendererPresentationUpdating = true;
            try
            {
                rendererUpdates.observe([[data, "propertyChange"]]);

            }
            finally
            {
                rendererPresentationUpdating = false;
            }
        }
    ]]></mx:Script>

    <mx:Canvas id="viewCanvas1" styleName="detailsContentBox" left="10" right="10" top="3" bottom="3">
        <mx:Canvas id="viewCanvas2" styleName="detailsSubContentBox" percentWidth="100" height="22">
            <ui:CustomLabel id="specialistType"  horizontalCenter="0" verticalCenter="0" />
        </mx:Canvas>
    </mx:Canvas>
    <ui:Frame id="specialistIcon" type="specialist" />
    <ui:StandardButton id="btnBuy" enabled="false" width="45" right="0" top="2" bottom="2" icon="{gAssetManager.GetClass('ButtonIconPay')}" />
    <local:RendererPoolBox id="costsList" direction="horizontal" top="32" horizontalCenter="0">
        <local:itemRenderer>
            <core:ClassFactory id="viewClassFactory1" />
        </local:itemRenderer>
    </local:RendererPoolBox>
</mx:Canvas>
