<?xml version="1.0" encoding="utf-8"?>
<mx:Canvas xmlns:mx="http://www.adobe.com/2006/mxml" xmlns:local="GUI.Components.ItemRenderer.*" implements="Model.Observer" width="200" height="160">
    <mx:Script><![CDATA[
        import mx.containers.Canvas;
        import Model.Observer;
        import GO.cGOSpriteLibContainer;
        import mx.core.mx_internal;
        import nLib.SpriteLibDataClass.dFrameCalculated;
        import flash.display.BitmapData;
        import flash.geom.Matrix;
        import Model.Notifier;
        import nLib.cSpriteLibContainer;
        import GO.cGO;
        import com.bluebyte.tso.util.ClientLogger;
        import flash.events.*;
        import flash.display.*;
        import flash.geom.*;
        import mx.styles.*;
        import flash.text.*;
        import flash.media.*;
        import mx.binding.*;
        import flash.filters.*;
        import flash.utils.*;
        import flash.net.*;
        import flash.system.*;
        import flash.accessibility.*;
        import flash.xml.*;
        import flash.ui.*;
        import flash.external.*;
        import flash.desktop.*;
        import flash.data.*;
        import flash.debugger.*;
        import flash.errors.*;
        import flash.filesystem.*;
        import flash.html.*;
        import flash.html.script.*;
        import flash.printing.*;
        import flash.profiler.*;
        import GUI.Components.RendererPresentationUpdates;
        import flash.events.Event;
        import flash.events.IEventDispatcher;

        private var spriteLibContainer:cGOSpriteLibContainer;

        public function set spriteName(argument1:String):void
        {
            this.data = argument1;
        }

        private function draw():void
        {
            var localValue1:dFrameCalculated = this.spriteLibContainer.getFrameCalculated(0, 0);
            if (((localValue1 == null) || (localValue1.orginalBitmap == null)))
            {
                return;
            };
            this.graphics.clear();
            var localValue2:BitmapData = localValue1.orginalBitmap;
            var localValue3:Number = (width / localValue2.width);
            var localValue4:Number = (height / localValue2.height);
            var localValue5:Number = 0;
            var localValue6:Number = 0;
            var localValue7:Number = ((localValue4 < localValue3) ? localValue4 : localValue3);
            localValue7 = Math.min(localValue7, 1);
            if (localValue7 == 1)
            {
                localValue5 = ((width / 2) - (localValue2.width / 2));
                localValue6 = ((height / 2) - (localValue2.height / 2));
            };
            var localValue8:Matrix = new Matrix(localValue7, 0, 0, localValue7, localValue5, localValue6);
            this.graphics.beginBitmapFill(localValue2, localValue8, false, false);
            this.graphics.drawRect(localValue5, localValue6, (localValue7 * localValue2.width), (localValue7 * localValue2.height));
            this.graphics.endFill();
        }

        public function update(argument1:Notifier, argument2:String, argument3:Object):void
        {
            this.draw();
        }

        override public function set data(argument1:Object):void
        {
            if (!rendererReady)
            {
                pendingRendererData = argument1;
                hasPendingRendererData = true;
                return;
            }
            var localValue2:String;
            if (this.spriteLibContainer != null)
            {
                this.spriteLibContainer.removePropertyObserver(cSpriteLibContainer.LOADING_DONE, this);
            };
            if ((argument1 is String))
            {
                localValue2 = String(argument1);
                if (global.buildingGroup.mGOListDictionary[localValue2] != null)
                {
                    this.spriteLibContainer = (global.buildingGroup.mGOListDictionary[localValue2] as cGOSpriteLibContainer);
                }
                else
                {
                    if (global.landscapeGroup.mGOListDictionary[localValue2] != null)
                    {
                        this.spriteLibContainer = (global.landscapeGroup.mGOListDictionary[localValue2] as cGOSpriteLibContainer);
                    }
                    else
                    {
                        if (global.streetGroup.mGOListDictionary[localValue2] != null)
                        {
                            this.spriteLibContainer = (global.streetGroup.mGOListDictionary[localValue2] as cGOSpriteLibContainer);
                        }
                        else
                        {
                            if (global.settlerGroup.mGOListDictionary[localValue2] != null)
                            {
                                this.spriteLibContainer = (global.settlerGroup.mGOListDictionary[localValue2] as cGOSpriteLibContainer);
                            }
                            else
                            {
                                if (global.animalGroup.mGOListDictionary[localValue2] != null)
                                {
                                    this.spriteLibContainer = (global.animalGroup.mGOListDictionary[localValue2] as cGOSpriteLibContainer);
                                };
                            };
                        };
                    };
                };
            }
            else
            {
                if ((argument1 is cGO))
                {
                    this.spriteLibContainer = cGO(argument1).GetGOContainer();
                };
            };
            if (this.spriteLibContainer != null)
            {
                super.data = argument1;
                this.spriteLibContainer.addPropertyObserver(cSpriteLibContainer.LOADING_DONE, this);
                this.draw();
            }
            else
            {
                ClientLogger.log(("MapitemRenderer cant find sprite from: " + argument1));
            };
            refreshRendererPresentation();
        }

        private var rendererReady:Boolean = false;
        private var pendingRendererData:Object;
        private var hasPendingRendererData:Boolean = false;
        private var rendererPresentationUpdating:Boolean = false;
        private var rendererUpdates:RendererPresentationUpdates;

        override protected function childrenCreated():void
        {
            super.childrenCreated();

            this.width = 200;
            this.height = 160;
            rendererReady = true;
            rendererUpdates = new RendererPresentationUpdates(this, refreshRendererPresentation);
            if (hasPendingRendererData)
            {
                var initialData:Object = pendingRendererData;
                hasPendingRendererData = false;
                pendingRendererData = null;
                data = initialData;
            }
            refreshRendererPresentation();
        }

        private function refreshRendererPresentation():void
        {
            if (!rendererReady || rendererPresentationUpdating) return;
            rendererPresentationUpdating = true;
            try
            {
                rendererUpdates.observe([[data, "propertyChange"]]);

            }
            finally
            {
                rendererPresentationUpdating = false;
            }
        }
    ]]></mx:Script>


</mx:Canvas>
